package com.affinitynow.app.utilisateur.dto;

import com.affinitynow.app.model.Topic;
import com.affinitynow.app.model.Utilisateur;
import org.junit.jupiter.api.Test;

import java.util.Set;

import static org.assertj.core.api.Assertions.assertThat;

class UtilisateurDtoTest {
/*
    @Test
    void should_convert_to_model() {
        // Given:
        final UtilisateurDto dto = new UtilisateurDto().setPseudo("007").setId(456L)
                .setTopics(Set.of(new RatedTopicDto().setName("T1"), new RatedTopicDto().setScore(2)));

        // When:
        final Utilisateur actual = dto.toUtilisateur();

        // Then:
        assertThat(actual).usingRecursiveComparison()
                .isEqualTo(new Utilisateur().setPseudo("007").setId(456L)
                        .setTopics(Set.of(new Topic().setName("T1"), new Topic().setScore(2))));
    }

    @Test
    void should_create_from_model() {
        // Given:
        final Utilisateur src = new Utilisateur().setPseudo("007").setId(456L)
                .setTopics(Set.of(new Topic().setName("T1"), new Topic().setScore(2)));

        // When:
        final UtilisateurDto actual = UtilisateurDto.from(src);

        // Then:
        assertThat(actual).usingRecursiveComparison()
                .isEqualTo(new UtilisateurDto().setPseudo("007").setId(456L)
                        .setTopics(Set.of(new RatedTopicDto().setName("T1"), new RatedTopicDto().setScore(2)))); }
*/
}